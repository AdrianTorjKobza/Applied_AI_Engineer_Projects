[project]
name = "TOONify"
version = "0.1.0"
description = "Optimize JSON for LLMs using TOON format"
dependencies = ["tiktoken"]
requires-python = ">=3.9"

[project.scripts]
toonify = "cli.main:main"

[tool.setuptools.packages.find]
include = ["toonify_ai*"]